---
envoy_admin_port: 9901
envoy_dependencies: ['apt-transport-https', 'ca-certificates', 'curl', 'gnupg2', 'software-properties-common']
envoy_etc_dir: /etc/envoy
envoy_generate_certificate: false
envoy_packages: ['getenvoy-envoy']
envoy_publish_to_datadog: false
envoy_service: envoy.service
envoy_state: started
envoy_upstreams: []  # list(dict(keys(hostname,port)))
...
