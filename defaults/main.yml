---
# --------------------------------

envoy_admin_port: 9901
envoy_clusters: []
envoy_dependencies:
  debian: ["apt-transport-https", "ca-certificates", "curl", "gnupg2", "software-properties-common"]
  redhat: ["ca-certificates", "curl", "gnupg2", "yum-utils"]
envoy_etc_dir: /etc/envoy
envoy_generate_certificate: false
envoy_listeners: []
envoy_main_port: 443
envoy_max_files: 8192
envoy_packages:
  debian: ["getenvoy-envoy"]
  redhat: ["getenvoy-envoy"]
envoy_publish_to_datadog: false
envoy_repo_key: "https://rpm.dl.getenvoy.io/public/gpg.CF716AF503183491.key"
envoy_repo_name: "tetrate-getenvoy-rpm-stable"
envoy_repo_url: "https://rpm.dl.getenvoy.io/public/config.rpm.txt?distro=el&codename=7"
envoy_svc_enabled: true
envoy_svc_name: envoy
envoy_svc_state: started

# --------------------------------
...
